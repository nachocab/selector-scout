{
  "name": "selector-scout",
  "version": "1.0.0",
  "private": true,
  "description": "A keyboard-driven DOM selector inspector for Chromium browsers.",
  "scripts": {
    "test": "node --test",
    "check": "node --check selector-scout.js && node --check service-worker.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
